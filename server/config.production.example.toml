# O origin fica acessível somente ao cloudflared na própria máquina.
listen_address = "127.0.0.1:8080"
database_path = "/var/lib/tempo-server/tempo-server.db"
online_timeout = "60s"
session_lifetime = "8h"

# Obrigatório quando o painel público usa HTTPS.
secure_cookies = true
